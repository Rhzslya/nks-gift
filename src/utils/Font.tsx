import { Bebas_Neue } from "next/font/google";

export const bebasNeue = Bebas_Neue({
  weight: ['400'], // Menentukan bobot yang digunakan
  subsets: ['latin'], // Menentukan subset karakter yang digunakan
  display: 'swap', // Opsi display untuk font
});